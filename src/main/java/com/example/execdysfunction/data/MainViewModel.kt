package com.example.execdysfunction.data

import androidx.lifecycle.ViewModel

class MainViewModel(private val studySessionDao: StudySessionDao) : ViewModel() {
    // Functions to interact with the StudySessionDao
}
